<!DOCTYPE html>
<html>
  <head>
    <title></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
  </head>
  <body class="jumbotron">
    <main>
      <h1>Gerenciamento de Produtos</h1>
      <a href="/form" class="btn btn-primary">Cadastrar produto</a>
      </br>
      <div class="container">
        <table class="table table-bordered">
          <thead>
            <tr>
              <th scope="col">id</th>
              <th scope="col">Nome</th>
              <th scope="col">Descrição</th>
              <th scope="col">Preço</th>
              <th scope="col">Quantidade</th>
              <th scope="col">Ações</th>
            </tr>
          </thead>
          <tbody>
            <% products.map((product) => { %>
            <tr>
              <td><%= product.id %></td>
              <td><%= product.name %></td>
              <td><%= product.descricao %></td>
              <td>R$<%= product.preco %></td>
              <td><%= product.quantidade %></td>
              <td>
                <a value="Deletar" class="btn btn-danger" href="/delete/produtos/<%= product.id %>">Deletar</a>
                <a value="Editar" class="btn btn-dark" href="/edit/produtos/<%= product.id %>">Editar</a>
              </td>
            </tr>
            <% }) %>
            
          </tbody>
        </table>
      </div>
    </main>
    <footer style="font-size: 14px;">
        <p>Desenvolvido por Anna Barbara Fragoso Curso: TSI  Matéria: Linguagem de Programação com Recursos de Segurança</p>
    </footer>
  </body>
</html>